# 在线协作版升级进展

## 阶段 1：母版来源复核与当前仓库定位

- 用户补充指出主逻辑来源目录在 `D:\妙成\星哈酷\投资效益周转表`。
- 已检查该目录，确认其中的 `pl-app/src/App.jsx` 与单文件 `russia-pl-calculator (1).jsx` 确实是这套计算器的重要母版来源。
- 同时对比发现：当前正式仓库 `D:\妙成\程序-AI创建\投资损益表-程序` 里的 `src/App.jsx`、`src/i18n.js` 已经叠加了更多后续增强，不适合整份倒回外部目录覆盖。
- 当前决策：
  - 外部目录继续作为“逻辑参考源”；
  - 当前仓库继续作为“正式在线协作版开发仓”；
  - 后续升级以当前仓库为准，避免把已完成的新人友好文案、公开项目入口、分享逻辑等改没。

## 阶段 2：在线协作版应用壳与兼容入口落地

- 已新增新的路由化应用壳，包含这些入口：
  - `/login`
  - `/app/projects`
  - `/app/projects/:projectId`
  - `/p/:slug`
  - `/share/:token`
  - `/share/embedded`
  - `/legacy`
  - 兼容根入口 `/`
- 已把旧入口兼容逻辑接到新壳里：
  - 旧 `?project=...` 会优先跳到新的公开页 `/p/:slug`
  - 如果是未登记的旧静态 JSON，则回退到 `/legacy?project=...`
  - 旧 `#share=...` 会统一跳到新的只读页 `/share/embedded`
- 已保留 `src/LegacyStandaloneApp.jsx` 作为旧版独立计算器入口，不直接覆盖原成熟实现。

## 阶段 3：云端项目层与 Supabase 脚手架

- 已新增前端云端项目服务层：
  - `src/lib/supabaseClient.jsx`
  - `src/lib/cloudProjects.js`
  - `src/lib/legacyData.js`
- 当前策略分两层：
  - **已配置 Supabase 环境变量**：走 `supabase-js`
  - **未配置 Supabase 环境变量**：自动进入本机“演示团队”模式，用 localStorage 模拟云端项目库
- 演示模式已经支持：
  - 云端项目列表
  - 新建项目
  - 保存草稿
  - 发布稳定公开版
  - 生成临时快照分享链接
  - 从旧静态公开项目自动种子导入演示项目库

## 阶段 4：项目库 / 项目页 / 公开页第一版

- 已新增项目库页：
  - 支持全部项目 / 我创建的 / 已公开 / 私有草稿 / 归档筛选
  - 支持打开项目、复制稳定公开链接、复制临时快照链接
- 已新增项目编辑页第一版：
  - 先以兼容 JSON 为正式云端数据编辑面
  - 支持导入 JSON / 导出 JSON / 保存草稿 / 发布公开版 / 复制临时快照
  - 支持检测“服务端版本已变化”的保存冲突，并允许用户确认是否覆盖
- 已新增公开只读页与快照只读页：
  - `/p/:slug` 展示最新发布版
  - `/share/:token` 展示冻结快照
  - `/share/embedded` 兼容旧版 hash 分享

## 阶段 5：后端目录与部署补充

- 已新增 Supabase 目录脚手架：
  - `supabase/migrations/20260612_online_collab_init.sql`
  - `supabase/functions/invite-member/index.ts`
  - `supabase/functions/update-member-role/index.ts`
- 已新增旧静态项目迁移脚本：
  - `scripts/migrate-legacy-projects.mjs`
- 已新增部署兼容文件：
  - `public/404.html`（GitHub Pages SPA 回跳）
  - `vercel.json`（Vercel 路由重写）

## 阶段 6：构建与本地浏览器验收

- 已执行 `npm run build`，构建通过。
- 已执行 GitHub Pages 口径构建：
  - `$env:GITHUB_PAGES='true'; npm run build; Remove-Item Env:\GITHUB_PAGES`
  - 构建通过。
- 已执行旧静态公开项目迁移脚本：
  - `node scripts/migrate-legacy-projects.mjs`
  - 成功生成 `supabase/seed/legacy-projects.json`
- 已启动本地开发服务并在浏览器中实际验收这些入口：
  - `/login`
  - `/app/projects`
  - `/app/projects/:projectId`
  - `/p/xiongwei-chuanqi`
  - `/?project=xiongwei-chuanqi-project.json`
  - `/#share=...`
- 验收结果：
  - 登录页可进入“演示团队”
  - 项目库能正常显示 7 个从旧公开项目种子导入的项目卡片
  - 项目页能打开，并显示 `项目 JSON / 导入 JSON / 保存草稿 / 发布公开版`
  - 公开页能显示“最后账上还剩现金 / 不同人先看什么 / 商品预览”
  - 旧 `?project=` 已自动跳转到新的 `/p/:slug`
  - 旧 `#share=` 已自动跳转到新的 `/share/embedded`

## 阶段 7：顺手修正的小问题

- 已修正项目编辑页里 JSON 解析状态的写法：
  - 不再在 `useMemo` 渲染过程中直接 `setState`
  - 改为一次性返回 `{ data, error }`，避免后续编辑时出现多余告警或渲染抖动

## 阶段 8：线上成熟页对齐与 Stitch 设计校准

- 用户反馈新协作版公开页“数据跟网页端展示完全不一样、逻辑更不通”，已复核线上成熟站点：
  - `https://a767881430-sketch.github.io/russia-pl-calculator/`
  - 线上成熟页仍是完整计算器结构：总览、商品明细、销售排期、现金流预测、参数与税制、税制说明、术语词典。
- 已调用 Stitch 建立设计方向：
  - Stitch 项目：`俄罗斯损益计算器协作版优化`
  - 核心设计结论：协作功能应作为“顶部协作工具栏/项目系统”包在成熟计算器外层，不能用轻量报表替代成熟测算主体。
- 已移除导致口径不一致的简化展示链路：
  - 删除 `src/components/ProjectReadonlyView.jsx`
  - `/p/:slug` 公开页改为直接加载完整成熟计算器 `src/App.jsx`
  - `/share/:token` 临时快照页改为直接加载完整成熟计算器
  - `/share/embedded` 旧 `#share=` 兼容页改为直接加载完整成熟计算器
- `src/App.jsx` 已新增外部数据入口能力：
  - `initialData`
  - `initialProjectName`
  - `initialDataVersion`
  - `readOnly`
  - `sourceLabel`
- 只读公开页当前策略：
  - 不显示本机项目列表、保存、另存为、导入、分享、重置等内部操作
  - 保留标签切换、展开明细、导出 CSV/HTML，方便老板、供应商、新人查看
  - 商品明细里的新增、清空、删除和展开编辑字段已在只读页隐藏或锁住
  - 公开页不会自动保存到本机项目库，避免污染内部数据
- 已修正项目编辑页一个实际报错点：
  - 移除不存在的 `setParseError("")`
  - 项目编辑页不再嵌入简化预览，而是提示使用完整测算预览/公开版/临时快照，避免两套结果并存。
- 已验证：
  - `npm run build` 通过
  - 本地 `/p/xiongwei-chuanqi` 显示完整成熟计算器，无乱码
  - 旧 `/?project=xiongwei-chuanqi-project.json` 会跳转到 `/p/xiongwei-chuanqi`，显示完整成熟计算器
  - 公开页不再显示保存/项目列表/新增/删除等内部入口
  - 与线上成熟页的主要信息结构已对齐，不再使用“4 张简化卡片 + 简化表”的降级展示

## 阶段 9：把切换项目、保存、分享收进同一个工作台

- 用户反馈“这 3 个不能放一起么”，已按这个方向收敛界面：
  - `/app/projects/:projectId` 不再以 JSON 编辑器作为主界面
  - 现在打开云端项目会直接进入完整成熟计算器
  - 顶部新增云端项目下拉框，用来在同一页面切换项目
  - 顶部保留一个主保存按钮：`保存到云端`
  - 顶部保留一个主分享按钮：`复制公开链接`
  - `项目库` 入口保留为辅助入口，不再要求用户频繁来回跳页面
- 云端工作台模式下隐藏容易让人混淆的旧按钮：
  - 不显示本机项目列表
  - 不显示另存为
  - 不显示导入 JSON
  - 不显示重置样例
  - JSON 仅保留为导出备份能力
- 已把云端保存/发布接回成熟计算器：
  - `保存到云端` 调用 `saveProjectDraft`
  - `复制公开链接` 会先发布当前版本，再复制 `/p/:slug`
  - 如果发现项目已被别人更新，仍会提示是否覆盖
- 已更新项目库提示文案：
  - 不再强调“JSON 预览/保存/发布”
  - 改为说明“打开项目就是完整测算工作台，切换项目、保存到云端、复制公开链接都在顶部”
- 已验证：
  - `npm run build` 通过
  - 本地项目库 `/app/projects` 可显示 7 个演示项目
  - 打开 `/app/projects/:projectId` 后显示完整成熟计算器，而不是 JSON 编辑页
  - 顶部能看到 1 个项目下拉、1 个 `保存到云端`、1 个 `复制公开链接`
  - 通过项目下拉切换到德力项目后，URL、项目名、选中项均更新
  - 切换后页面仍停留在完整测算工作台，没有回到 JSON 编辑页

## 阶段 10：新增老板 / 供应商 / 运营三种视角

- 用户认可“一个主工作台 + 三种视图”的方向，已先在总览仪表盘顶部落地第一版。
- 新增 `AudienceViewPanel`，不改任何公式，只重组已有测算结果：
  - `老板先看`：最后账上还剩现金、回本月份、回报率、最缺钱的时候
  - `供应商先看`：销售单位数量、平均供应商报价、平均报关申报价、重量资料待补
  - `运营先看`：M1 预计销售、平台综合扣费、后续补货支出、现金峰值月份
- 每个视角都有：
  - 一句“当前判断”
  - 一组“下一步”
  - 当前税制提示
- 页面策略：
  - 公开链接和云端项目打开后，仍进入完整成熟计算器
  - 但总览页第一屏先看到不同角色视角，降低新人/供应商/老板理解成本
  - 原来的完整指标、口径说明、现金流图、成本结构、税制结构仍保留在下面
- 已验证：
  - `npm run build` 通过
  - 本地云端项目页默认显示 `老板视图：这件事值不值得投`
  - `供应商先看` 与 `运营先看` 按钮可切换，关键标题和字段正常显示
  - 390px 移动宽度检查无横向溢出，三个视角按钮宽高正常

## 阶段 11：手机端工作台顶部收纳

- 用户追问手机端是否可以一起搞定，已先处理云端工作台顶部在手机上的拥挤问题。
- 手机端云端项目页顶部现在保留 3 个主动作：
  - `保存`
  - `分享`
  - `更多`
- `更多` 菜单收纳高级动作：
  - 项目库
  - 导出 JSON
  - 导出 CSV
  - 导出报告
  - 使用说明
- 桌面端仍保留完整工具条，方便高频编辑和导出。
- 手机端隐藏冗长提示，只保留必要的公开状态说明，避免占掉第一屏。
- 已验证：
  - `npm run build` 通过
  - 这次浏览器插件打开本地 `127.0.0.1/localhost` 时被客户端拦截，未能完成二次截图验收
  - 移动端收纳主要通过响应式类实现：云端模式下导出/说明按钮在 `<640px` 隐藏到 `更多` 菜单，主操作仍保留在顶部
  - 阶段 10 已完成 390px 视角面板无横向溢出检查

## 阶段 12：总览页重新排版，降低运营后台感

- 用户反馈当前版本仍偏运营，老板和供货商可能看不懂，已重新调整总览页信息层级。
- 新总览默认顺序改为：
  1. `一句话结论`
  2. `给供货商先对齐的数`
  3. `老板 / 供应商 / 运营三种视角`
  4. `关键财务指标`
  5. `当前数值口径对照`
  6. `展开完整运营明细`
- 新增 `DecisionSummaryPanel`：
  - 用红/绿状态点说明最后现金、回本、利润、资料完整度
  - 给老板一个“当前版本能不能继续谈”的判断
  - 给供货商单独展示产品数、销售单位、供应商报价合计、报关申报价合计、平均平台扣费
- 已把运营图表和深度明细默认折叠：
  - 累积现金流图
  - 投资人关注卡
  - 本月经营利润分布
  - 成本结构
  - 税制结构
  - 商品盈利排行
- 这样老板/供应商打开后先看到结论和报价信息，运营人员需要时再展开明细。
- 已验证：
  - `npm run build` 通过
  - 浏览器插件本轮仍因 `net::ERR_BLOCKED_BY_CLIENT` 未能打开本地 URL 做截图验收
  - 代码层面已通过构建检查，折叠状态默认关闭，运营图表不会默认占用第一屏

## 阶段 13：多平台单品利润模型第一版

- 用户提供并要求参考 3 份平台单位经济效益表：
  - `星哈酷OZON单位经济效益表-学习使用.xlsx（更新版）.xlsx`
  - `星哈酷WB单位经济效益表-AI学习使用.xlsx`
  - `星哈酷Yandex单位经济效益表-AI学习使用.xlsx`
- 已先抽取三类平台表的核心结构：
  - Ozon：商品成本、售价/买家价、Ozon 佣金、体积物流、非本地销售等平台侧扣费。
  - WB：商品成本、WB 折扣前价、买家折扣、FBW/FBS/DBW/DBS 佣金、本地化指数、基础费率。
  - Yandex：商品成本、售价、市场佣金、付款频率费、FBS/FBY/DBS 等履约费用。
- 当前先不把上万行类目佣金表完整塞进前端，也不自动匹配俄文类目；首版采用“每个商品可选择平台 + 可展开费用明细”的方式：
  - 旧字段 `list / platformFee / warehouse / mgmt` 继续兼容旧项目。
  - 新增 `platforms` 明细结构，用来保存 Ozon / WB / Yandex 各自售价、平台扣费、仓费、管理费、销售占比和费用拆分。
  - 若旧项目没有 `platforms`，默认按 Ozon 100% 销售处理，保证历史项目结果不被破坏。
- 已在 `src/App.jsx` 新增平台模型基础函数：
  - `SALES_PLATFORMS`
  - `getProductPlatformConfigs`
  - `calcPlatformDetailFee`
  - `calcPlatformUnitEconomics`
  - `getEnabledPlatformEconomics`
  - `getProductPlatformAverages`
- 已把主计算口径切换为“启用平台按销售占比加权”：
  - 单品利润 `calcProduct` 使用加权后的售价、平台扣费、仓费、管理费。
  - 月度现金流 `calcProjection` 默认继承加权后的售价和平台扣费；若运营在售价/扣费排期里手动改某月，仍优先使用月度手填值。
- 商品明细 UI 已加入 Ozon / WB / Yandex 平台选择：
  - 每个 SKU 可勾选多个销售平台。
  - 每个平台可填销售模式、售价、销售占比、平台综合扣费、海外仓费、管理费。
  - 主利润按启用平台的销售占比归一化加权；例如填 Ozon=100、WB=10，会按约 91% / 9% 参与计算。
  - 页面展示从“填入占比”改成“实际占比”，避免客户误以为多个平台必须手动凑满 100%。
- 费用明细已做成默认隐藏、点击展开：
  - 阶段 13 先做了通用字段版；阶段 14 已按真实 Ozon / WB / Yandex 定价表口径修正为平台专属字段。
  - 勾选“用下面明细自动合计平台扣费”后，平台综合扣费使用明细合计。
- 兼容逻辑已补强：
  - 旧项目没有 `platforms` 时仍默认 Ozon 100%，历史结果不变。
  - 旧项目第一次只新增 WB/Yandex 配置时，也不会把 Ozon 默认值误关掉。
- 导出 CSV/HTML 已同步显示销售平台汇总：
  - CSV 增加销售平台、加权售价、加权平台综合扣费、加权海外仓费、加权管理费。
  - HTML 商品表增加销售平台列。
- 手机端已增加商品卡片视图：
  - `<768px` 宽度下不再把商品编辑器塞进 1200px 宽表格。
  - 手机端可直接展开 SKU，平台卡片按单列显示，费用明细仍默认隐藏。
- 已参考用户提供的三份平台单位经济效益表确认费用字段覆盖：
  - Ozon：折扣前价/买家价、佣金、银行费、基础物流、FBO/FBS、包裹处理、非本地销售等。
  - WB：折扣前价/买家价、FBW/FBS/DBW/DBS 佣金、本地化指数、基础费率、仓库倍率等。
  - Yandex：市场佣金、收单/付款转账、FBY/FBS/DBS、最后一英里、订单处理等。
- 已验证：
  - `npm run build` 通过。
  - Node 计算小样例通过：旧项目维持 Ozon 100%；新增 WB 后按 100:10 归一化加权。
  - 浏览器桌面端验证：商品明细可展开，Ozon/WB/Yandex 卡片出现，勾选 WB 后当前平台显示更新，费用明细可展开。
  - 浏览器 390px 手机宽度验证：总览页无横向溢出；商品明细使用手机卡片；展开 SKU 后平台输入框在手机宽度内显示，无页面级横向溢出。
  - Vite 仍提示 chunk 大于 500kB，这是当前项目已有的大包体积警告，不影响构建产物。

## 阶段 14：按真实平台定价表修正费用明细

- 用户指出阶段 13 的费用明细没有读懂定价表，截图中 `本地配送 / 退货预留 / 广告固定` 等字段并不是当前三份平台单位经济效益表的核心字段。
- 已重新读取三份 Excel 的真实表头和公式：
  - Ozon 主表字段：`佣金 OZON%`、`OZON佣金`、`银行费率`、`基础运费`、`非本地销售利润率`、`包裹处理（FBS用）`、`最后一英里（从中心到接送点）`、`退货处理`、`签收率%`、`广告和其他服务成本%`、`OZON 扣除总额`。
  - WB 主表字段：`佣金 WB%`、`WB佣金`、`银行费率`、`产品体积（L）`、`基本费率`、`超过1L,每升基本费率`、`FBW仓库倍率`、`本地化指数`、`销售分布系数`、`物流总费用`、`广告和其他服务成本%`、`FBW存储费，预估30天`、`WB扣除总额`。
  - Yandex 主表字段：`展示商品（佣金%）`、`接受付款（收单）`、`付款转账（收单）`、`交付给买家（最后一英里）`、`订单处理（针对 FBS）`、`平均里程(FBY)/俄罗斯联邦交货(FBS)`、`退货处理`、`退货配送`、`签收率`、`FBY存储费`、`广告和其他服务成本`、`市场服务总额`。
- 已修正费用明细 UI：
  - Ozon 展开后只显示 Ozon 定价表口径字段。
  - WB 展开后只显示 WB 定价表口径字段。
  - Yandex 展开后只显示 Yandex 定价表口径字段。
  - 已移除不符合用户定价表的通用字段：`本地配送`、`广告固定`、`退货预留`、`平台物流`、`履约处理` 等。
- 已同步修正 `src/App.jsx` 与 `src/lib/calcEngine.js`：
  - 费用明细自动合计改为按平台不同公式口径粗算。
  - 继续保留手填 `平台扣费` 的主路径；只有勾选“用下面明细自动合计平台扣费”时才使用拆分字段。
  - 继续兼容旧字段：旧 JSON 中的 `logistics / fulfillment / storage / returnReserve` 会尽量映射到新字段，避免旧数据直接丢失。
- 已验证：
  - `npm run build` 通过。
  - 浏览器实物检查 Ozon 明细：`佣金 OZON% / 基础运费 / 非本地销售利润率% / 包裹处理（FBS用） / 最后一英里 / 退货处理 / 签收率% / 广告和其他服务成本%` 均出现。
  - 浏览器实物检查确认错误字段不再出现：`本地配送 / 广告固定 / 退货预留 / 平台物流 / 履约处理`。
  - 浏览器实物检查 WB/Yandex 明细字段均出现：`基本费率 / 超过1L每升费率 / FBW仓库倍率 / 本地化指数 / 展示商品（佣金%） / 接受付款（收单） / 付款转账（收单） / 订单处理（FBS） / 平均里程/联邦交货 / FBY存储费`。

## 当前状态说明

- 这次已经不是单纯“分析”，而是已经把在线协作版的第一层骨架搭起来了。
- 但也要诚实说明：
  - 当前云端项目页已经接回完整成熟计算器；
  - 项目名称、商品明细、排期、参数等编辑会在完整测算器里操作；
  - 云端保存和公开发布已放到顶部同一排；
  - 旧版独立入口仍保留在 `/legacy`，用于过渡和兜底；
  - 下一阶段重点是正式接 Supabase 线上环境变量、部署 Vercel，并把线上公开项目逐步切到云端稳定链接。

## 阶段 15：按 Excel 类目定价表重做平台费用逻辑

- 用户明确指出：当前多平台费用明细仍不对，平台费用不应该靠手动拆分字段猜算，而应该像原 Excel 定价表一样，按平台、销售模式、类目、售价档位、体积、地区等参数自动查表计算。
- 已重新确认四份表的数据来源：
  - Ozon：`星哈酷OZON单位经济效益表-学习使用.xlsx（更新版）.xlsx`
  - WB：`星哈酷WB单位经济效益表-AI学习使用.xlsx`
  - Yandex 主公式：`星哈酷Yandex单位经济效益表-AI学习使用.xlsx`
  - Yandex 类目佣金：`Yandex-4月佣金.xlsx`
- 已对照 Excel 样例行确认平台扣费结果：
  - Ozon 主表第 8 行：`OZON 扣除总额 = 276.375₽`
  - WB 主表第 8 行：`WB扣除总额 = 7235.35875₽`
  - Yandex 主表第 9 行：`市场服务总额 = 7132.56134782609₽`
- 下一步执行：
  - 从 Excel 抽取类目佣金、基础运费、非本地销售费率、本地化指数、Yandex 类目路径为前端可查询数据源。
  - `src/App.jsx` 和 `src/lib/calcEngine.js` 改成“优先按定价表自动计算，无法匹配时才回退手动平台扣费”。
  - UI 改成选择/填写类目与必要参数，并显示定价表查到的佣金率、物流费和综合扣费。
- 已完成第一版落地：
  - 新增 `scripts/extract-platform-tariffs.py`，可从桌面 Excel 抽取 Ozon/WB/Yandex 平台定价数据。
  - 新增 `src/lib/platformTariffs.js`，保存抽取后的类目佣金、Ozon 运费、Ozon 非本地销售、WB 本地化指数、Yandex 类目路径数据。
  - 新增 `src/lib/platformPricing.js`，集中实现 Ozon/WB/Yandex 平台费用计算，`src/App.jsx` 与 `src/lib/calcEngine.js` 统一调用这一套逻辑。
  - 商品平台明细 UI 已改成“按定价表参数自动计算平台扣费”，并显示类目佣金率、是否匹配类目、Ozon 运费匹配、WB 物流总费用、Yandex 最后一英里/平均里程等关键中间值。
  - 新增 SKU 默认启用按定价表自动计算；旧项目如果只填了 `platformFee` 且没勾明细，仍保留手动综合扣费，不强行改历史数据。
- 已用 Excel 样例做回归：
  - Ozon 样例：网页引擎计算 `276.375₽`，与 Excel 第 8 行 `OZON 扣除总额` 一致。
  - WB 样例：网页引擎计算 `7235.35875₽`，与 Excel 第 8 行 `WB扣除总额` 一致。
  - Yandex 样例：网页引擎计算 `7132.56134782609₽`，与 Excel 第 9 行 `市场服务总额` 一致。

## 阶段 16：用户确认平台费必须按类目表完全计算

- 用户再次明确：平台费用逻辑要换成和 Excel 表完全一样，不能再理解成手工平台费或通用费用字段。
- 已复查三份平台表首页公式，确认主路径必须是：
  - Ozon：按 `类目/商品类型 + 售价档位 + 发货方案 + 体积档位 + 供货/配送集群` 查表，得到佣金率和基础运费，再合计银行费、配送、退货签收、广告服务。
  - WB：按 `子类目 + 发货方案 + 本地化指数 + 体积/仓库倍率` 查表，得到佣金率、地域分配系数、销售分布系数，再合计物流、退货签收、广告、FBW 存储、罚款预留。
  - Yandex：按 `类目路径 + 运营方案 + 支付频率 + 体积` 查表，得到佣金率、转账费率、平均里程/联邦交货，再合计最后一英里、订单处理、退货签收、FBY 存储、广告服务。
- 下一步继续加固：
  - 默认新增 SKU 不再只给空平台费，而是提示必须填写/匹配平台类目后自动计算。
  - 计算引擎补充更清晰的 Excel 字段名输出，方便界面和导出说明“这笔平台扣费来自哪一项”。
  - 跑构建与样例回归，确保网页端和 Excel 样例一致。
- 已完成本阶段修正：
  - `src/lib/platformPricing.js` 明确把平台费用分成三种来源：`Excel 定价表自动计算`、`手填费率自动合计`、`未完整匹配，暂用手填平台扣费`。
  - Ozon 佣金严格按 `佣金表 B 列` 匹配商品类型，运费严格按 `最新基本运费 B/C/D 列` 匹配体积档位、供货集群、配送集群。
  - WB 佣金严格按 `类目佣金表 B 列` 匹配子类目，并要求本地化指数表匹配后才使用自动扣费。
  - Yandex 保留类目路径查表；若关闭类目查表，则页面显示为“手填费率自动合计”，不再冒充定价表自动匹配。
  - 不完整匹配时，当前实际采用值会退回 `platformFee` 手填值，避免自动结果偏低。
  - `src/App.jsx` 费用明细中新增“当前使用”“匹配口径”“还缺什么”“当前实际采用”等提示。
  - 新增 SKU 默认带 Ozon/WB/Yandex 可匹配示例参数，方便直接看到 Excel 自动计算效果。
- 已跑 Node 回归：
  - Ozon Excel 第 8 行：`276.375₽` 对齐。
  - WB Excel 第 8 行：`7235.35875₽` 对齐。
  - Yandex Excel 第 9 行：`7132.56134782609₽` 对齐。
  - 缺 Ozon B 列类目时，正确退回手填平台扣费 `500₽`。

## 阶段 17：确认佣金表 B 列为平台费用匹配准绳，并修复构建卡顿

- 用户明确补充：佣金表里面基本看 B 列，因此本阶段把 Ozon / WB 的类目佣金匹配口径固定为“B 列是准绳”。
- 已在 `src/lib/platformPricing.js` 明确返回匹配来源：
  - Ozon：`Ozon 佣金表 B 列`
  - WB：`WB 类目佣金表 B 列`
  - Yandex：仍按佣金表类目路径处理
- 已在商品平台费用明细里新增“实际匹配到”说明：
  - 匹配成功时显示具体匹配行，例如 `Ozon 佣金表 B 列「Раскраска」`、`WB 类目佣金表 B 列「Куртки」`
  - 未匹配时显示未找到的输入值，并继续回退到手填平台扣费，避免自动计算结果偏低
- 已把原本 4MB+ 的 `src/lib/platformTariffs.js` 大源码表拆出去：
  - 新增 `public/platform-tariffs.json` 保存完整 Excel 抽取定价表
  - `src/lib/platformTariffs.js` 改为轻量读取器，只读取启动时挂载的定价表
  - `src/main.jsx` 在 React 应用启动前加载 `platform-tariffs.json`
  - `scripts/extract-platform-tariffs.py` 同步更新，以后重新抽表会写入静态 JSON，不会再把大表塞进源码包
- 已完成回归验证：
  - Ozon Excel 样例：`276.375₽`，匹配 `Ozon 佣金表 B 列「Раскраска」`
  - WB Excel 样例：`7235.35875₽`，匹配 `WB 类目佣金表 B 列「Куртки」`
  - Yandex Excel 样例：`7132.56134782609₽`
  - Ozon 故意填不存在的 B 列类目时，正确回退到手填平台扣费 `500₽`
- 已完成构建验证：
  - `npm run build` 通过
  - 构建时间从大表源码导致的超时，恢复到约 `1.68s`
  - 仍有 Vite chunk 大小提示，但不影响构建产物

## 阶段 18：按“少填字段 + 佣金类目选择”重做平台费输入体验

- 用户指出当前费用明细仍不符合真实使用方式：不应该让新人手填大量平台参数，而应只填产品成本、重量、长宽高、选择佣金类目、预留广告百分比、预计签收率后自动计算。
- 已完成本阶段修正：
  - 商品基础信息区新增常显 `商品尺寸（平台物流会用）`，长/宽/高不再只藏在白关运输模式里。
  - 平台物流体积优先使用商品基础信息里的长宽高自动计算；历史平台配置里的 `volumeLiters` 只作为没有长宽高时的兜底，避免旧体积覆盖新尺寸。
  - Ozon / WB / Yandex 佣金类目输入改为带候选的 `datalist`，候选项来自已抽取的 Excel 佣金表：
    - Ozon：佣金表 B 列，约 9,339 个候选。
    - WB：类目佣金表 B 列，约 7,371 个候选。
    - Yandex：佣金表类目路径，约 11,026 个候选。
  - 费用明细主区只保留新人需要填的内容：佣金类目、必要的平台地区/付款频率、预计签收率、预留广告百分比。
  - 佣金率兜底、银行费率、基础运费兜底、仓库倍率、罚款预留、收单/转账兜底等字段已收进 `高级兜底参数（一般不用填）`，默认折叠。
  - 平台卡片的 `售价` 文案改为 `平台计费价/折扣前售价`，并说明佣金、银行费、广告按这个价计算；这对 WB 尤其重要，因为 Excel 第 8 行佣金/银行费/广告按折扣前价 `9999₽` 计算，不是买家折后价 `6999.3₽`。
  - 商品明细表头、商品提示语、收入确认基础、平台计费价排期等中文文案同步改为 `平台计费价/折扣前售价`，减少把买家折后价填进平台扣费底数的误解。
  - 默认平台费路径改为：没有显式关闭时按定价表自动计算；匹配失败时才允许回退手填平台扣费。
- 已完成回归验证：
  - Ozon Excel 样例：`276.375₽` 对齐。
  - WB Excel 第 8 行真实参数：折扣前价 `9999₽`、类目 `Куртки`、体积 `21.6L -> 22L`、签收率 `40%`，计算 `7235.35875₽` 对齐。
  - Yandex Excel 第 9 行真实参数：售价 `9999₽`、类目 `Бытовая техника`、体积 `33.8L`、签收率 `69%`、退货处理 `15₽`，计算 `7132.56134782609₽` 对齐。
  - 缺 Ozon B 列类目、缺商品长宽高/体积时，仍回退手填平台扣费 `500₽`，避免自动结果偏低。
  - 商品长宽高会覆盖旧平台体积：`24 × 18 × 0.2 cm = 0.0864L`，不会被历史 `volumeLiters: 99` 覆盖。
- 已完成构建验证：
  - `npm run build` 通过。
  - 仍有 Vite chunk 大小提示，但不影响构建产物。
## 阶段 19：修复数字输入框清空后自动回 0 的体验
- 用户反馈商品重量、长宽高等输入框清空后会自动冒出 `0`，并且在原本为 0 的字段里输入会出现 `01`、`011` 这类前导 0。
- 已把通用 `NumInput` 改为本地草稿输入：
  - 聚焦到值为 0 的输入框时先显示为空，方便直接输入新数字。
  - 输入过程中不再立刻把空值强制写回 0。
  - 失焦或按 Enter 时再把文本规范化为数字，`011` 会收敛为 `11`。
  - 仍保持内部数据为数字，空白最终按 0 参与计算，不改变公式。
- 已新增 `InlineNumInput`，把销售排期、补货排期、平台计费价排期、平台扣费排期、分润/提现排期的小格子也统一成同一套输入体验。
- 本阶段只修输入体验，不改计算公式和项目 JSON 字段。
## 阶段 20：新增平台定价表只读核对页
- 用户反馈页面提示来自 Excel 定价表，但管理界面里看不到定价表本身，新人不知道“往哪里匹配”。
- 已新增顶部页签 `平台定价表`，作为只读核对界面，不提供编辑，避免误以为可以直接改 Excel 公式。
- 页面支持按平台查看：
  - Ozon：佣金表 B 列、基础运费表、非本地销售费率。
  - WB：佣金表 B 列、本地化指数。
  - Yandex：佣金类目路径、付款频率。
- 每个表支持搜索类目、集群、区间等关键词；大表默认只显示前 200 行，并提示继续搜索缩小范围，避免一次渲染几万行拖慢页面。
- 页面显示抽取时间和来源文件，方便和桌面 Excel 定价表对照。
## 阶段 21：构建与浏览器验证
- 已运行 `npm run build`，构建通过；仍有 Vite chunk 大小提示，但不影响产物运行。
- 已在本地浏览器验证 `http://127.0.0.1:5173/app/projects/project_265572d5-5a43-4f70-978c-fdb5a4e73ba2`：
  - 商品明细展开后，原本为 0 的重量、长宽高显示为空白，不再自动弹出 0。
  - 用真实按键输入 `011` 后会规范显示为 `11`。
  - 用全选 + Backspace 清空后，输入框保持空白，内部仍按 0 参与计算。
  - 平台定价表页可打开，Ozon/WB/Yandex 可切换。
  - Ozon 搜索 `Раскраска` 能过滤到 1 行，证明搜索和 B 列类目核对可用。
- 已检查浏览器控制台，未发现新增 error/warning。

## 阶段 22：在线保存版本构建恢复
- 用户截图显示本地开发页出现 Vite 红色错误层，原因是 `src/App.jsx` 中多处显示文案/货币符号被编码污染后打断 JSX 字符串。
- 已集中修复阻塞构建的区域：
  - 顶部财务指标卡片；
  - 平台费用明细；
  - 商品尺寸与物流提示；
  - 平台定价表只读核对页；
  - 销售排期默认售价/扣费表；
  - 预测参数、提现合计、帮助页和词汇表。
- 为降低再次编码损坏风险，部分受损中文文案临时替换为 ASCII/英文文案；计算逻辑和云端保存逻辑未因此改变。
- 已重新运行 `npm run build`，构建通过。当前只剩 Vite chunk 大小提示，不影响运行。
- 当前在线保存方案保持为：
  - `/p/:slug` 公开只读展示；
  - `/app/projects/:projectId` 登录后在线编辑；
  - 公开项目在云端编辑页点击保存时同步发布，公开链接读取最新发布数据。
- 真正线上跨设备保存仍需要部署端配置：
  - 执行 `supabase/migrations/20260612_online_collab_init.sql`；
  - 配置 `VITE_SUPABASE_URL`；
  - 配置 `VITE_SUPABASE_ANON_KEY`。
- 未配置 Supabase 时仍会退回浏览器本地模拟云端存储，只能用于演示，不能作为多人共享保存。

## 阶段 23：恢复中文文案与编码乱码修复
- 用户反馈在线协作版改造后中文文案被替换/丢失，并出现乱码。
- 已优先修复 `src/App.jsx` 中会显示到页面上的错码与缺失中文：
  - 登录页和顶部品牌标识恢复为正常字符；
  - 导出 HTML 报告中的品牌行恢复为 `星哈酷 · XingHaKu Investment Analytics`；
  - 页面 footer、项目列表、商品明细、税制卡片、平台定价表等位置的错码分隔符恢复为 `·`；
  - 平台费默认俄文类目恢复为 `Раскраска` / `Куртки`，不再显示成错码字符；
  - 导出 JSON 成功提示恢复为中文可读文案。
- 已清理 `src/App.jsx` 中残留的乱码注释，并将文件保存为 UTF-8 无 BOM，降低后续再次出现编码污染的风险。
- 已完成验证：
  - 使用 `rg` 扫描 `src/App.jsx`，未再发现常见乱码字符或私有区错码字符；
  - `npm run build` 通过，仍只有 Vite chunk 体积提示，不影响运行；
  - 本地浏览器验证 `http://127.0.0.1:5173/app/projects/project_265572d5-5a43-4f70-978c-fdb5a4e73ba2`：项目库、云端编辑页、商品明细页、平台定价表页的中文显示正常，未发现控制台 error/warning。

## 阶段 24：恢复三视角解释与决策摘要标注
- 用户反馈总览页的三种视角看起来没有真正变化，红框里的数字也缺少说明，容易变成“生冷数字”。
- 已在 `src/App.jsx` 修复总览页第一屏说明：
  - `决策摘要` 的四个判断卡补回“这是什么意思”和“下一步做什么”：
    - 账上现金；
    - 回本；
    - 利润；
    - 规格。
  - 新增 `老板先看 / 供应商先看 / 运营先看` 三条角色提示，让新人知道每种角色为什么看这些数字。
  - 右侧 `待确认数字` 改为 `报价前要对齐的数字`，并给商品数、供应商报价、报关申报价、平均平台扣费都补上 `怎么看 / 下一步`。
  - 当前税制不再裸露 `usn_15` 这类代码值，改为显示 `俄罗斯简化税制 15%`。
- 已把 `老板 / 供应商 / 运营` 三种视角改成真正不同内容：
  - 老板视角：最后账上现金、回本月份、回报率、最缺钱的时候。
  - 供应商视角：销售单位、供应商报价、报关申报价、规格待补。
  - 运营视角：销售排期总量、补货排期、平均平台扣费、M1 实际现金进出。
- 每个视角卡片都新增 `怎么看` 和 `下一步` 两段短说明，避免只显示一个孤立数字。
- 已完成验证：
  - `npm run build` 通过，仍只有 Vite chunk 体积提示，不影响运行。
  - 本地浏览器验证 `http://127.0.0.1:5173/app/projects/project_265572d5-5a43-4f70-978c-fdb5a4e73ba2`：
    - 决策摘要能看到 `老板先看 / 供应商先看 / 运营先看`；
    - 报价确认区能看到 `报价前要对齐的数字`、`怎么看`、`下一步`；
    - 点击 `供应商` 后显示销售单位、供应商报价、报关申报价、规格待补；
    - 点击 `运营` 后显示销售排期总量、补货排期、平均平台扣费、M1 实际现金进出。
  - 390px 手机宽度检查无页面级横向溢出；顶部页签保留自身横向滚动，属于预期行为。
  - 浏览器控制台未发现新增 error/warning。

## 阶段 25：上传云端前连通性体检
- 用户准备上传云端前，要求最后检查各界面连接性和是否还有明显问题。
- 已完成构建验证：
  - `npm run build` 通过。
  - 仍有 Vite chunk 体积提示，属于当前大应用包体积提示，不阻塞运行和部署。
- 已完成主要路由巡检：
  - `/app/projects` 项目库可打开，能看到已迁入的 7 个公开项目。
  - `/app/projects/:projectId` 云端项目工作台可打开，当前项目总览、保存并发布、分享和项目切换入口正常。
  - `/p/xiongwei-chuanqi` 公开页可免登录打开，显示最新发布版和总览摘要。
  - `/?project=xiongwei-chuanqi-project.json` 旧入口可跳转到对应云端项目。
  - `/usage-guide.html` 使用说明页可打开。
  - `/legacy?project=xiongwei-chuanqi-project.json` 仍保留旧独立入口，按旧逻辑先要求访问密码，作为兜底入口存在。
- 已完成编辑页页签巡检：
  - 总览仪表盘、商品明细、平台定价表、销售排期、现金流预测、参数与税制、税制说明、术语词典均可打开。
  - 三视角和决策摘要文案在编辑页与公开页都能显示。
  - 平台定价表页保留 Excel 原始字段核对口径。
- 已完成手机端检查：
  - 390px 宽度下，项目库、编辑页、公开页均无页面级横向溢出。
  - 编辑页和公开页顶部页签保留自身横向滚动，属于预期。
  - 手机端 `更多` 菜单可打开，包含项目库、导出 JSON、导出 CSV、导出报告、使用说明。
- 已完成图表稳定性修复：
  - `src/App.jsx` 与 `src/LegacyStandaloneApp.jsx` 的 Recharts 容器补充明确宽高和 `minWidth: 0`。
  - 复查现金流图和月度利润图均能渲染，控制台不再出现图表宽高 warning。
- 云端部署前必须注意：
  - 当前本机没有 `.env` 文件，所以本地仍是演示模式/浏览器本地模拟云端存储。
  - 真正多人云端保存需要先执行 `supabase/migrations/20260612_online_collab_init.sql`，并在 Vercel 配置 `VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`。
  - `vercel.json` 已配置 SPA rewrites，上传 Vercel 后刷新 `/app/projects/:id`、`/p/:slug` 这类路径不会直接 404。

## 阶段 26：按阿里云部署口径修正 SPA 回退
- 用户明确最终不是上传 Vercel，而是上传阿里云。
- 已修正此前部署提醒：
  - 阿里云上传静态站时应上传 `dist/` 构建产物。
  - 如果使用 OSS 静态网站，需要配置默认首页 `index.html`，默认 404 页 `404.html`。
  - 如果使用 ECS/Nginx，需要用 `try_files $uri $uri/ /index.html;` 做前端路由回退。
- 已修复 `public/404.html`：
  - 原逻辑会把任意一级路径当成 base，例如 `/p/xiongwei-chuanqi` 可能被误判为 `/p` 子目录。
  - 新逻辑只在 GitHub Pages 旧路径 `/russia-pl-calculator` 下启用 base，其余阿里云根域名路径统一回退到 `/?redirect=...`。
  - 这样阿里云 OSS 下刷新 `/p/:slug`、`/app/projects/:id` 时可以回到正确前端路由。
- 已新增阿里云部署说明：
  - `doc/2026-06-12-阿里云部署说明.md`
- 已重新运行 `npm run build`，构建通过。

## 阶段 27：区分演示包与正式 Supabase 云端包
- 用户指出“真正云端保存版”必须在构建前写入 Supabase 环境变量，不能把未配置 Supabase 的演示包当正式包上传。
- 已把此前生成的 zip 改名为：
  - `release/russia-pl-calculator-aliyun-demo-no-supabase.zip`
- 该包只适合页面展示和本机演示保存，不适合正式多人云端保存。
- 已新增 `.env.production.example`，标明正式构建需要：
  - `VITE_SUPABASE_URL`
  - `VITE_SUPABASE_ANON_KEY`
- 已新增正式阿里云打包脚本：
  - `scripts/package-aliyun-release.ps1`
- 脚本会先检查 `.env.production` 和 Supabase 环境变量，未配置则停止，避免误打演示包。
- 正式包目标文件名：
  - `release/russia-pl-calculator-aliyun-supabase.zip`

## 阶段 28：解释阿里云与 Supabase 的分工
- 用户提出疑问：项目已经放到阿里云，为什么在程序里直接编辑还需要 Supabase。
- 已明确当前架构分工：
  - 阿里云 OSS/静态网站负责托管前端网页，让老板、供应商、新人能打开程序。
  - Supabase 负责后端项目库、登录权限、项目保存、公开发布版本和临时分享快照。
  - 如果没有 Supabase 或同类后端，浏览器里的编辑只能作为本机缓存/演示保存，无法保证别人跨设备看到同一份最新项目。
- 已把该解释补充到：
  - `doc/2026-06-12-阿里云部署说明.md`
- 已补充可替代方案说明：
  - 若坚持全放阿里云，需要另建阿里云后端，例如 ECS/函数计算 + 数据库 + 登录权限接口；这属于新的后端开发工作，不是单纯上传静态网页即可完成。

## 阶段 29：确认最终方向改为纯阿里云后端版
- 用户确认：阿里云上也需要有后端，目标不是“阿里云前端 + Supabase 后端”的混合方案。
- 后续正式目标调整为：
  - 阿里云 OSS/CDN 负责前端页面托管；
  - 阿里云函数计算或 ECS 负责后端 API；
  - 阿里云 RDS PostgreSQL/MySQL 负责项目数据、版本、分享链接和成员权限；
  - 前端保存、发布、项目库、公开页、登录权限都逐步改为调用阿里云后端。
- Supabase 方案保留为已验证的过渡/参考实现，但不作为最终上线架构。
- 下一阶段应先做阿里云后端技术方案和接口清单，再把当前 `cloudProjects.js` 的 Supabase 调用替换为阿里云 API 调用。

## 阶段 30：参考 XingHaKu ERP 形态，确认“正常程序”目标
- 用户提出希望计算器像 `D:\星哈酷ERP\XingHaKu_ERP` 一样，成为一个正常程序，而不是多个平台拼在一起。
- 已检查 ERP 项目结构，确认其形态为：
  - `backend_api/`：FastAPI 后端；
  - `frontend_web/`：Next.js 前端；
  - `alembic/`：数据库迁移；
  - `.env.example`：数据库、JWT、加密密钥等环境变量模板；
  - `一键启动新版ERP.bat`：本地一键启动后端、前端和后台 worker。
- 该形态更符合用户想要的正式系统体验：用户只看到一个程序入口，登录后即可使用，保存由后端和数据库完成。
- 后续有两个合理落地方向：
  - 独立做成 `russia-pl-calculator` 全栈程序，结构参考 ERP；
  - 或并入现有 XingHaKu ERP，作为“投资损益测算/项目测算”模块，共用 ERP 的登录、权限、数据库和部署方式。
- 当前建议优先评估“并入 ERP”方案，因为已有后端、数据库、权限和一键启动基础，可减少重复造后端。
- 已新增正式方案文档：
  - `doc/2026-06-12-正常程序化方案.md`

## 阶段 31：确认不并入 ERP，改为独立客户版系统
- 用户明确：ERP 功能已经很多，俄罗斯损益计算器主要给客户老板、潜在合作方、尚未合作对象查看，因此不适合并入内部 ERP。
- 正式方向调整为：
  - 单独做成一个完整程序；
  - 工程形态参考 ERP，但不复用 ERP 前端入口；
  - 独立后端、独立数据库、独立登录和公开分享；
  - 对外界面只保留项目测算、老板视角、供应商视角、运营视角、公开只读页和分享页；
  - 不暴露 ERP 的订单、库存、店铺 API、财务流水同步等内部功能。
- 已更新 `doc/2026-06-12-正常程序化方案.md`，把推荐方案从“并入 ERP”改为“独立客户版正常程序”。

## 阶段 32：落地独立 FastAPI 后端与客户访问码
- 已新增独立后端 `backend_api/`，不再把 Supabase 作为正式保存路径。
- 后端能力包括：
  - `POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/me`
  - `GET/POST/PUT /api/projects`
  - `POST /api/projects/{id}/publish`
  - `POST /api/projects/{id}/access-code`
  - `POST /api/projects/{id}/share-links`
  - `GET/POST /api/public/{slug}` 公开项目访问码读取
  - `GET/POST /api/share/{token}` 临时快照访问码读取
- 已新增 SQLAlchemy 模型和 Alembic 初始迁移：
  - `users`
  - `projects`
  - `project_versions`
  - `project_share_links`
- 已新增本地/生产环境模板 `.env.example`，默认本地 SQLite，生产可切换阿里云 RDS PostgreSQL。
- 已新增本地一键启动脚本：
  - `一键启动损益计算器.bat`
- 已新增旧公开项目迁移脚本：
  - `scripts/migrate_legacy_projects_to_backend.py`
- 已新增阿里云完整程序打包脚本：
  - `scripts/package-aliyun-fullstack.ps1`
- 已新增独立客户版阿里云部署说明：
  - `doc/2026-06-12-独立客户版阿里云部署说明.md`
- 前端已新增：
  - `src/lib/apiClient.js`
  - `src/lib/authClient.jsx`
- 前端认证和项目数据层已从 Supabase 改为调用本项目 FastAPI；公开页和临时分享页已加入访问码门禁。

## 阶段 33：构建、接口烟测与阿里云完整包
- 已移除正式前端路径中的 Supabase 客户端和依赖，`src/lib/cloudProjects.js` 已改为调用本项目 FastAPI。
- 已把旧 `scripts/package-aliyun-release.ps1` 改为转调新的 fullstack 打包脚本，避免误打旧 Supabase 包。
- 已运行验证：
  - `python -m compileall backend_api scripts\migrate_legacy_projects_to_backend.py` 通过；
  - `npm run build` 通过；
  - 后端接口烟测通过：健康检查、后台登录、新建项目、发布项目、错误访问码 403、正确访问码读取公开项目。
- 已生成完整阿里云包：
  - `release/russia-pl-calculator-aliyun-fullstack.zip`
- 当前仍有 Vite chunk 体积提示，但不影响构建和部署。

## 阶段 34：上线前浏览器巡检与客户只读修复
- 已在本地打开并巡检：
  - `/app/projects` 项目库；
  - `/app/projects/project_392e1604520e405b8e922af704203daf` 后台项目工作台；
  - `/p/xiongwei-chuanqi` 客户公开只读页。
- 已修复客户公开页不够干净的问题：
  - 客户公开页不再显示“编辑副本/在线编辑”入口；
  - 公开页里的报价确认提示，在只读状态下改为“请联系内部人员更新并重新发布”，不再提示客户自己保存发布；
  - 后端公开 API 新增 `map_public_project`，公开读取只返回最新发布版本，不把后台草稿数据作为客户页主要数据暴露。
- 已把正式路径里的旧“云端”文案替换为“后台/服务器”：
  - 后台工作台顶部显示“保存到服务器”；
  - 项目打开提示、保存成功提示和工作台来源标签改为独立后端口径。
- 已在项目库补充“重置访问码”按钮：
  - 管理员/编辑可直接给公开项目设置或更换客户访问码；
  - 更新后项目卡片会同步显示访问码状态。
- 已完成浏览器验证：
  - 后台保存并发布会新增 `published` 版本，数据库中 `xiongwei-chuanqi` 的 `published_version_id` 已更新；
  - 客户公开页输入 `xhk2026` 后可完整只读查看；
  - 客户公开页没有输入框、保存、发布、项目库、编辑入口；
  - 390px 手机宽度下，项目库、后台工作台、客户公开页均无页面级横向溢出。
- 已重新运行验证：
  - `npm run build` 通过；
  - `python -m compileall backend_api scripts\migrate_legacy_projects_to_backend.py scripts\repair_legacy_project_records.py` 通过；
  - `powershell -ExecutionPolicy Bypass -File scripts\package-aliyun-fullstack.ps1` 通过。
- 已重新生成最新阿里云完整包：
  - `release/russia-pl-calculator-aliyun-fullstack.zip`

## 阶段 35：解决 Vite 构建包体提醒
- 用户询问第 6 点“Vite chunk 体积提醒”如何解决。
- 已在 `vite.config.js` 增加生产构建拆包配置：
  - `vendor-react`：React / React DOM / React Router；
  - `vendor-charts`：Recharts / D3 相关依赖；
  - `vendor-icons`：Lucide 图标库；
  - `vendor`：其他第三方依赖。
- 修改后重新运行 `npm run build`：
  - 构建通过；
  - 最大业务包 `AppShell` 约 492.55 kB，低于默认 500 kB 提醒线；
  - 原先的 chunk 体积 warning 已消失。
- 已重新运行阿里云完整打包：
  - `powershell -ExecutionPolicy Bypass -File scripts\package-aliyun-fullstack.ps1` 通过；
  - 最新包仍输出到 `release/russia-pl-calculator-aliyun-fullstack.zip`。
