# 2026-06-12 雄伟传奇静态展示上传进展

## 阶段 1：仓库与文件检查

- 已确认当前工作目录：`D:\妙成\程序-AI创建\投资损益表-程序`。
- 已确认 Git 远端：
  - `origin` 指向 `https://github.com/a767881430-sketch/russia-pl-calculator.git`
- 已确认桌面待上传文件存在：
  - `C:\Users\Administrator\Desktop\雄伟传奇-new.JSON`
  - 文件大小约 2.1 KB
- 已确认旧静态页面当前已经有公开入口：
  - `public/xiongwei-chuanqi-project.json`
  - `src/App.jsx` 的公开项目列表里已有“雄伟传奇”
  - `public/usage-guide.html` 里也已有“雄伟传奇”打开入口
- 当前策略：覆盖旧的 `public/xiongwei-chuanqi-project.json`，保持旧静态链接不变，方便继续用原页面展示。

## 阶段 2：静态项目数据替换

- 已用 `C:\Users\Administrator\Desktop\雄伟传奇-new.JSON` 覆盖：
  - `public/xiongwei-chuanqi-project.json`
- 覆盖后旧链接保持不变：
  - `/?project=xiongwei-chuanqi-project.json`
- 已用 Node 解析校验 JSON：
  - `projectName`：雄伟传奇
  - 商品数：6
  - 测算周期：8 个月
  - 第一个商品：23597（三合一）
- 与旧公开 JSON 相比：
  - 商品数从 2 个增加到 6 个
  - 测算周期从 6 个月改为 8 个月
  - 原有两个 SKU 的采购价、售价、平台费、数量等已按新文件更新
- 已运行 `npm run build`，构建通过。
- 构建仍提示 `AppShell` chunk 大于 500 KB，这是项目既有体积提示，不影响本次静态 JSON 展示上传。

## 阶段 3：提交与推送

- 已只暂存本次相关文件：
  - `public/xiongwei-chuanqi-project.json`
  - `doc/2026-06-12-雄伟传奇静态展示上传.md`
- 未把工作区里已有的其他未提交改动纳入本次提交。
- 已创建提交：
  - `1d9e284 Update Xiongwei Chuanqi static project`
- 已推送到 GitHub：
  - `origin/main`
- 当前远端 `main` 已包含新版雄伟传奇静态项目 JSON，可继续用旧静态页面链接展示。

## 阶段 4：网页修改后无法保存的排查

- 用户反馈：在网页上修改数据后无法保存。
- 已排查入口逻辑：
  - 访问 `/?project=xiongwei-chuanqi-project.json` 时，当前新版入口会先识别公开项目，并跳转到 `/p/xiongwei-chuanqi`。
  - `/p/:slug` 是公开展示页，`PublicProjectPage.jsx` 会以 `readOnly` 模式打开计算器。
  - `readOnly` 模式会隐藏保存按钮，并且不会把修改写入本机或云端。
- 已排查旧静态入口：
  - `/legacy?project=xiongwei-chuanqi-project.json` 仍会打开旧版可编辑计算器。
  - 旧版保存逻辑写入浏览器 `localStorage`，属于“保存到本机”，不会直接改 GitHub 仓库里的 JSON 文件。
- 结论：
  - 公开展示链接不能直接保存回 GitHub，这是当前设计限制，不是 JSON 上传失败。
  - 如果只是临时改数，应进入旧静态编辑副本或云端工作台；如果要让所有人看到新版，需要把修改后的 JSON 再提交/推送到仓库，或走云端发布流程。
- 下一步处理：
  - 在公开展示页增加清晰入口，让用户能从只读页进入旧静态可编辑副本，避免误以为公开页能直接保存。

## 阶段 5：改为支持线上直接编辑保存

- 用户进一步明确：需要“网上直接能修改保存”，不是本机浏览器副本。
- 已确认技术边界：
  - GitHub Pages 静态页面不能直接把修改写回仓库 JSON。
  - 要做到多人/多设备都能看到最新版，必须使用后端存储；本项目已具备 Supabase 云端项目库雏形。
- 已调整实现方向：
  - 公开页 `/p/:slug` 继续作为外部只读展示页。
  - 管理员从公开页可进入 `/app/projects/:projectId` 在线编辑页。
  - 登录后访问旧 `?project=xiongwei-chuanqi-project.json` 链接时，会优先跳到对应云端项目编辑页。
  - 如果云端项目库为空，项目库页会自动把旧静态公开项目导入云端项目。
  - 已公开项目在云端编辑页点击保存时，会同步发布最新版，公开链接会看到最新数据。
- 已补正式 Supabase 登录链路：
  - 登录后读取真实 `workspace_members` 身份。
  - 新增 `ensure_default_workspace()` 数据库函数，首次登录用户会自动创建默认工作区并成为管理员，避免正式环境无法保存。
- 注意：
  - 线上共享保存需要部署 Supabase 表结构，并在部署平台配置 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`。
  - 未配置 Supabase 时仍是演示模式，保存只会写到当前浏览器的模拟云端存储，不能跨设备共享。


## 阶段 6：在线直接修改保存可行性确认
- 用户明确要求“网上直接能修改保存”。
- 已确认静态 GitHub Pages / 静态 JSON 不能直接保存回仓库，也不能多人跨设备共享最新修改。
- 当前实现已转向 Supabase 云端保存方案：公开页只读展示，登录后的 `/app/projects/:projectId` 作为在线编辑工作台。
- 已修复本轮 `src/App.jsx` 编码污染造成的 Vite 编译错误，`npm run build` 已通过。
- 上线后要真正保存到云端，需要配置 Supabase 表结构和 `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`；未配置时只能使用浏览器本地模拟存储。

## 阶段 7：本地预览验证在线编辑入口
- 已启动 `npm run preview -- --host 127.0.0.1 --port 4173`。
- 已打开 `/p/xiongwei-chuanqi`，页面正常渲染，不再出现 Vite 红色编译错误层。
- 公开页已出现 `Online edit` 入口，并指向 `/app/projects/:projectId`。
- 未登录访问编辑页会跳转到 `/login`，符合在线保存需要身份的设计。
- 使用演示管理员进入后，编辑页显示 `保存并发布` 按钮。
- 点击 `保存并发布` 后，页面无前端报错；未配置 Supabase 时走本地模拟云端存储，正式线上保存仍需配置 Supabase。

## 阶段 8：按截图修改雄伟传奇公开项目参数
- 用户要求把公开链接 `https://a767881430-sketch.github.io/russia-pl-calculator/?project=xiongwei-chuanqi-project.json` 里的两个选项改成截图红框所示状态并保存。
- 已确认截图对应的内部字段：
  - 税制选择：`params.taxScheme` 从 `usn_15` 改为 `usn_6`，即“俄罗斯简化税制（USN）6%：按收入”。
  - 收入确认基础：`params.incomeBasis` 从 `payout` 改为 `list`，即“全款上架价（M × 售价）”。
- 已更新静态公开项目文件：
  - `public/xiongwei-chuanqi-project.json`
- 本阶段只修改用户指定的两个参数，商品、汇率、货损率、预测期等其他数据保持不变。
